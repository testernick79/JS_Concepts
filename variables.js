// CONST USAGE

/*
//once assigned cannot be re-assigned
const numbers = 1234;
console.log(numbers);

const numbers = 1000;

// allows you to reassign values within the const
const settings = {
    number: 12,
    time: 'noon'
};

settings.number = 11;
settings.time = 'eleven';
console.log(settings);
*/
// LET USAGE

let i = 2;
i = 3;


console.log(i);